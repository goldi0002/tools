{
  "name": "toolkit",
  "private": true,
  "version": "0.1.0",
  "description": "ToolKit AI: a growing collection of AI-powered and classic utilities.",
  "scripts": {
    "client:dev": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "client:preview": "npm --prefix client run preview",
    "server:dev": "npm --prefix server run dev",
    "server:start": "npm --prefix server start",
    "build": "npm --prefix client install && npm --prefix client run build && rm -rf server/public && cp -r client/dist server/public",
    "start": "npm --prefix server start"
  }
}
